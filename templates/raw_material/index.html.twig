{% extends 'base-admin.html.twig' %}

{% block title %}RawMaterial index{% endblock %}

{% block body %}
    <h1>RawMaterial index</h1>

    <div class="mb-3">
        <a class="btn btn-primary" href="{{ path('raw_material_new') }}">
            <i class="fa fa-plus"></i>
            {{'Create new'|trans}}</a>
    </div>

    {% for message in app.flashes('error') %}
    <div class="alert alert-danger">
        {{ message }}
    </div>
    {% endfor %}

    <table class="table sortable-table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for raw_material in raw_materials %}
            <tr>
                <td>{{ raw_material.id }}</td>
                <td>{{ raw_material.name }}</td>
                <td>
                        <a class="btn btn-primary btn-sm mr-2" href="{{ path('raw_material_show', {'id': raw_material.id}) }}">
                            <i class="fa fa-eye"></i>
                        </a>
                        <a class="btn btn-primary btn-sm mr-2" href="{{ path('raw_material_edit', {'id': raw_material.id}) }}">
                            <i class="fa fa-pencil"></i>
                        </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
